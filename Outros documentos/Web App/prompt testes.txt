pretendo que implementes testes e2e para esta funcionalidade de login, faz para a pagina Login,e para o componente de navegaçao. nao precisas de fazer testes de performance, nem de responsividade rapida da pagina. nao te esqueças de testar o toast. usa a abordagem que considerares melhor (se usas o apimock ou api interceptor ) mas tenta usar os page objects e as features com page manager apimock e apiinterceptor(se usares). divide por secçoes logicas com test.describe( se apropriado usa locators de acessibilidade getbyrole ou com data-testid, ou seja , testes que sejam estaveis, seja qual for a abordagem que uses) aceda ao filesystrem e edite/adicione os ficheiros que precisar nos locais apropriados use sempre "'**/animals?**'" nunca apenas "**/animals" porque faz sempre redirect para animals?page=1. não uses "await page.waitForLoadState('networkidle')" espera por um elemento qualquer seja renderizado ou usa outra forma, esperar por url, ou respostw api ou etc..., usar o         await apiMock.clearMocks(); quando necessário. ao usar o mock e por a retornar json, cuidado ao chamar page.navigatetologin() porque não renderiza o componente apenas ´devolve o json. ai deves navegar para home (que não retorna json)e usar o site para navwgar para o login, ou antao mockar so ¨/api/login" ee não intersetar o /login do frontend(ve qual a melhor opção, se esta ultima funcionar, usa a ultima)
respostas da api: 

*****************